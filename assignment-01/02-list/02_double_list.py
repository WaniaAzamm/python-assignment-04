def main():
    numbers = [1, 2, 3, 4]

    for i in range(len(numbers)):
        numbers[i] = numbers[i] * 2

    print(numbers)

if __name__ == '__main__':
    main()
# This code defines a function `main` that doubles each number in the list `numbers` and prints the modified list.